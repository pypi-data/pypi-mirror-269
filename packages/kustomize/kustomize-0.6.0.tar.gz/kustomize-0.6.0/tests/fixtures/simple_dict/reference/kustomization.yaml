# Example configuration for the webserver
# at https://github.com/monopole/hello
commonLabels:
  app: hello

resources:
- deployment${PATH_SEPARATOR}my_deployment.yaml
- service${PATH_SEPARATOR}my_service.yaml
- configMap${PATH_SEPARATOR}my_config_map.yaml
