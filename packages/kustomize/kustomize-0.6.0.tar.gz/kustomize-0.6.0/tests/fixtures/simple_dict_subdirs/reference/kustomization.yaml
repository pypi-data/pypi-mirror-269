# Example configuration for the webserver
# at https://github.com/monopole/hello
commonLabels:
  app: hello

resources:
- deployments${PATH_SEPARATOR}deployment${PATH_SEPARATOR}my_deployment.yaml
- services${PATH_SEPARATOR}service${PATH_SEPARATOR}my_service.yaml
- configmaps${PATH_SEPARATOR}configMap${PATH_SEPARATOR}my_config_map.yaml
