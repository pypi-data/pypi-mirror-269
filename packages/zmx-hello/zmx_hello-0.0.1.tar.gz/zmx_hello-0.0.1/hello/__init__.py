from .main import hello

# it tell python the whole directory should be treated as a package and it controlled what is available when that package is imported

# by importing the file to the __init__.py we can let users to access the main.py module functions from the package