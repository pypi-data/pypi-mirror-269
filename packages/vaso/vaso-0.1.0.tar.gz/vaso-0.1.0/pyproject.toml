[tool.poetry]
name = "vaso"
version = "0.1.0"
description = "A simple packaging mechanism for arbitrary files."
authors = ["Matt Christie <christiemj09@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
