[tool.poetry]
name = "testpkg5_ftcao"
version = "0.1.0"
description = "Testing package in clean environment"
authors = ["Frank Cao"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
