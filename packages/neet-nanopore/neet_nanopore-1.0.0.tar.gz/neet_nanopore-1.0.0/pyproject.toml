[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neet-nanopore"
version = "1.0.0"

dynamic = ["dependencies", 
    "scripts", 
    "classifiers", 
    "authors", 
    "license", 
    "requires-python", 
    "readme"]