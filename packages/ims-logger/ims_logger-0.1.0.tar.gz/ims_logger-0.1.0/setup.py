from setuptools import find_packages, setup

setup(
    name='ims_logger',
    packages=find_packages(include=['ims_logger']),
    version='0.1.0',
    author='Allen',
    description='Library created for logging in IMS application'
)