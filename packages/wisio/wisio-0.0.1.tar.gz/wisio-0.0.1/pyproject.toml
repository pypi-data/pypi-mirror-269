[build-system]
requires = ["cmake>=3.24", "scikit-build>=0.17", "setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff.format]
quote-style = "preserve"

[tool.ruff.lint.pycodestyle]
max-line-length = 100
