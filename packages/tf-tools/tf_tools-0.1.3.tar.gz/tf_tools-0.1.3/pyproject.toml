[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tf-tools"
version = "0.1.3"
authors = [
  { name="Marcel Claramunt", email="marcel@moveread.com" }
]
description = "Useful tools for `tensorflow`, typed with `jaxtyping`"
dependencies = [
  "jaxtyping", "tensorflow", "lazy-loader", "keras"
]
requires-python = ">=3.10"
readme = {file="README.md", content-type="text/markdown"}

# [project.urls]
# repo = "<GIT_REPO_URL>"
