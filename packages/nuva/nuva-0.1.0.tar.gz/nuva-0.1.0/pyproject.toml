[project]
name = "nuva"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "AdamZou", email = "yz725@cornell.edu" }
]
dependencies = [
    "langchain>=0.1.15",
    "pytest>=8.1.1",
    "pandas>=2.2.2",
    "codeinterpreterapi-yayi>=0.1.15",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/tet"]
