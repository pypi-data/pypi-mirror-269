[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "happyscript"
description = "Framework for running scripts"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.5"
dependencies = [ ]

